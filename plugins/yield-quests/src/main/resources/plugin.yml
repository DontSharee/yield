name: yield-quests
version: '${version}'
main: me.dontshare.yieldquests.YieldQuests
api-version: '26.2'
depend: [yield-packs, yield-zones, yield-rebirth, yield-skilltree]
softdepend: [HeadDatabase]
permissions:
  yieldquests.admin:
    default: op
