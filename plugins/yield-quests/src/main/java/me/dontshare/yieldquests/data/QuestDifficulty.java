package me.dontshare.yieldquests.data;

/** The 3 tiers every quest category offers - a player commits to exactly one per category per day (see QuestService). */
public enum QuestDifficulty {
    EASY,
    MEDIUM,
    HARD
}
