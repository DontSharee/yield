package me.dontshare.yieldachievements.potion;

/** Every stat a potion can boost - mirrors yield-skilltree's own NodeType multiplier categories, since potions stack multiplicatively on top of those same totals. */
public enum PotionStat {
    COINS,
    DAMAGE,
    LUCK,
    ROLL_SPEED
}
