package me.dontshare.yieldachievements.store;

/** Which Store tab a {@link StoreProduct} shows under - a permanent rank, or everything else (a gamepass-style perk, a consumable potion). */
public enum StoreProductCategory {
    RANK,
    GAMEPASS
}
